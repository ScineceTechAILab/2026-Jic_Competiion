1. while you solve any cli problem , you must summary the problem and the solution into a MD doc (using the template)

2. if the cli is related to building something, you must make the build output to be in the cache folder.

3. cache file naming format: build_CLIName_CLIFirstParam (e.g. build_ros2_colcon)
